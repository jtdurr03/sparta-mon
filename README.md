# sparta-mon

A hacker-style, ncurses-based system monitor for Raspberry Pi / Debian Linux.

## Build (from source)

### Dependencies
```bash
sudo apt update
sudo apt install -y build-essential libncursesw5-dev

eof
